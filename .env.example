# ====== General ======
# Python will not read this automatically; use `dotenv` in apps/services if needed.
# For GitHub Actions, prefer encrypted Secrets instead of committing real values.

# Human-readable UA for geocoding providers (required by Nominatim ToS)
GEOCODER_USER_AGENT="clm-mvp/0.1 (+https://your-org.example)"

# Optional HTTP proxy (if your network requires one)
HTTP_PROXY=""
HTTPS_PROXY=""

# ====== Data Paths ======
# Base directory where EU sample/mirror datasets are stored
DATA_DIR="data/eu"

# Specific dataset file locations (override if you store them elsewhere)
CORINE_PATH="data/eu/corine/corine_sample.gpkg"
NATURA_PATH="data/eu/natura2000/natura2000_sample.gpkg"
WATERBASE_PATH="data/eu/waterbase/waterbase_sample.csv"

# ====== Rules Engine ======
# Default rulepack used by the screening
RULES_DIR="src/rules/configs"
RULEPACK_FILE="sfdr_pai7_v1.yaml"

# ====== Mapping / UI ======
# Optional map token (if you later add a basemap provider that requires it)
MAPBOX_TOKEN=""

# ====== App ======
# Streamlit app settings
STREAMLIT_PORT=8501
STREAMLIT_SERVER_HEADLESS=true
